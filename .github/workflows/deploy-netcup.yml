name: Obsolete static deployment (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Static FTPS deployment is no longer compatible
    runs-on: ubuntu-latest

    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "The former static FTPS deployment is intentionally disabled."
          echo "Punktlandung now requires the Next.js API routes and WebSocket server on the VPS."
          echo "Use the reviewed VPS release process instead."
